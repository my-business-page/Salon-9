const loader=document.getElementById('loader');window.addEventListener('load',()=>setTimeout(()=>loader?.classList.add('hide'),450));const menu=document.querySelector('.menu'),nav=document.querySelector('nav');menu?.addEventListener('click',()=>{nav?.classList.toggle('open');menu.setAttribute('aria-expanded',nav?.classList.contains('open')?'true':'false')});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));const progress=document.querySelector('.progress i'),header=document.querySelector('.header');const reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max?scrollY/max*100:0)+'%';header?.classList.toggle('scrolled',scrollY>35)};addEventListener('scroll',update,{passive:true});update();const items=document.querySelectorAll('.reveal');if(!reduced){const io=new IntersectionObserver(entries=>entries.forEach(entry=>{entry.target.classList.toggle('visible',entry.isIntersecting);entry.target.classList.toggle('exit',!entry.isIntersecting&&entry.boundingClientRect.top<0)}),{threshold:.12,rootMargin:'-7% 0px -7% 0px'});items.forEach((el,i)=>{el.style.transitionDelay=(i%5)*65+'ms';io.observe(el)});document.querySelectorAll('.service-card,.visit-art,.hero-art').forEach(card=>card.addEventListener('pointermove',e=>{if(innerWidth<850)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${y*-4}deg) rotateY(${x*5}deg) translateY(-5px)`}));document.querySelectorAll('.service-card,.visit-art,.hero-art').forEach(card=>card.addEventListener('pointerleave',()=>card.style.transform=''));}document.querySelectorAll('a[href^="https://wa.me/"]').forEach(a=>a.setAttribute('aria-label','Contact Poodle Nail House on WhatsApp'));document.querySelectorAll('a[href^="tel:"]').forEach(a=>a.setAttribute('aria-label','Call Poodle Nail House'));